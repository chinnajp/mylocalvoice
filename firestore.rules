rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open for local emulator / MVP citizen reporting.
    // Tighten for production cloud (see FIREBASE_SETUP.md).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
